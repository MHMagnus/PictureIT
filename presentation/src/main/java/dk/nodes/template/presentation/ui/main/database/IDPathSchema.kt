package dk.nodes.template.presentation.ui.main.database

class IDPathSchema {
    object IDPathTable {
        const val NAME = "IDPathTable"

        object Cols {
            const val IMAGEID = "imageID"
            const val PATH = "path"
        }
    }
}